# Streamlit Community Cloud prefers a requirements.txt in the app's own directory over the repo
# root, so this is the one it reads. It's a deliberate subset: only what app/dashboard.py's serving
# chain (api.scoring, schemas, reason codes and model artifacts) needs at runtime. Training-only
# packages are left out on purpose, keeping the hosted app small and its dependency graph clear.
pandas==3.0.5
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
lightgbm==4.6.0
shap==0.46.0
matplotlib==3.9.2
pyparsing==3.2.5
fastapi==0.141.1
streamlit==1.62.0
joblib==1.5.3
httpx==0.28.1
pydantic==2.13.4
