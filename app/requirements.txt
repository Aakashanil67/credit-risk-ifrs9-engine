# Streamlit Community Cloud prefers a requirements.txt in the app's own directory over the repo
# root, so this is the one it reads. It's a deliberate subset: only what app/dashboard.py's import
# chain (api.scoring -> src.explain -> src.features/config/data_loader/preprocessing) needs at
# runtime. Training-only packages are left out on purpose. optbinning drags in ortools, which wants
# numpy>=2.0.2 and collides with the numpy==1.26.4 pin below; before this file existed that
# collision made the platform's resolver give up entirely. See DECISIONS.md.
pandas==2.2.3
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
lightgbm==4.5.0
shap==0.46.0
matplotlib==3.9.2
fastapi==0.115.5
streamlit==1.40.1
joblib==1.4.2
httpx==0.27.2
pydantic==2.9.2
